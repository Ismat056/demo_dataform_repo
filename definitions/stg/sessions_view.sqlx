config {
  type: "view",
  name: "session_counts_by_day"
}

SELECT
  event_date,
  COUNT(*) AS session_count
FROM
  ${ref("events_*")}
WHERE
  event_name = "session_start"
GROUP BY
  event_date
ORDER BY
  event_date